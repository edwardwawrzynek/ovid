// __ovid_compiler_test
//__mode: compile
//__end_header

fn inc2(a i32) -> i32 {
    return inc(inc(a))
}

fn inc(a i32) -> i32 {
    return a + a
}

fn test(a bool) -> bool {
    if a {
        return test(a)
    } else {
        return false
    }
}
