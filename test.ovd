val a i32 = 5
mut sink *i32 = &a

fn foo(a **i32) {
    sink = *a
}

fn test() -> *i32 {
    val a = 5
    val b = (&a, a)
    val c = &b
    return (*c).0
}

/*
FUNCTION std:scope:test:
	%a flows to *(%12).0
	*(%12) flows to *(%b)
	%b flows to *(%c)
	*(*(%c)).0 flows to *(%RETURN)
*/