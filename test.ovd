val a i32 = 5
mut sink *i32 = &a

fn foo(a **i32) {
    sink = *a
}

fn test() {
    val var = 5
    val addr = &var
    val arg = &addr
    foo(arg)
}

fn test2(a **(i32, **(*(u8, *i32), *i32))) -> *i32 {
    val b = (**(**a).1).0
    return (*b).1
}

fn test3(a **(i32, *i32)) -> *i32 {
    return (**a).1
}